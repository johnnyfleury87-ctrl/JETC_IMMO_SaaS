# ========================================
# JETC_IMMO - Configuration Environnement
# ========================================
# IMPORTANT: Copier ce fichier en .env.local et remplir avec vos vraies valeurs
# Ne JAMAIS commit .env.local dans Git

# MODE DE FONCTIONNEMENT
# Valeurs possibles: "demo" | "pro"
# demo = simulation frontend uniquement, aucune écriture en base
# pro = mode réel avec authentification et données persistées
MODE=demo

# ========================================
# SUPABASE - Configuration
# ========================================
# Documentation : https://supabase.com/dashboard/project/_/settings/api
# Récupérer les valeurs depuis : Dashboard > Settings > API

# 1️⃣ URL du projet Supabase
# Où trouver : Dashboard > Settings > API > Project URL
# Format : https://xxxxxxxxxxxxx.supabase.co
# Exposition : ✅ PUBLIC (utilisé frontend + backend)
SUPABASE_URL=https://votre-projet.supabase.co

# 2️⃣ Clé publique ANON (exposée au navigateur)
# Où trouver : Dashboard > Settings > API > Project API keys > anon public
# Exposition : ✅ PUBLIC (protection par RLS)
# Utilisée par : Frontend (src/lib/supabaseClient.js)
# Variable requise : NEXT_PUBLIC_* pour exposition frontend
NEXT_PUBLIC_SUPABASE_ANON_KEY=votre_anon_key_ici

# 3️⃣ Clé ADMIN SERVICE_ROLE (BACKEND UNIQUEMENT)
# Où trouver : Dashboard > Settings > API > Project API keys > service_role (cliquer "Reveal")
# Exposition : ❌ SECRET (BACKEND UNIQUEMENT)
# Utilisée par : Backend (api/_supabase.js)
# ⚠️ NE JAMAIS exposer cette clé au frontend
# ⚠️ Cette clé bypass TOUS les Row Level Security (RLS)
# ⚠️ NE JAMAIS commit cette clé dans Git
SUPABASE_SERVICE_ROLE_KEY=votre_service_role_key_ici

# Clé d'installation pour créer le premier admin JTEC
# ⚠️ Générer une clé aléatoire forte (minimum 32 caractères)
# ⚠️ À SUPPRIMER après création du premier admin pour sécurité
# Générer avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
INSTALL_ADMIN_KEY=669460b9ad6d812681f1f89564c9d22f49efffbd88e609e4de66406607a89bf0

# VERCEL / DEPLOYMENT
# Port de développement local
PORT=3000

# LANGUES SUPPORTÉES
# FR, EN, DE
DEFAULT_LANGUAGE=fr
